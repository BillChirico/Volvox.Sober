{
  "name": "@volvox-sober/types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo 'No linting configured for types package'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests for types package (type definitions only)'"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
